// Optional dev auth credentials for Firebase email/password sign-in.
// Leave empty to use anonymous auth (ensure Anonymous is enabled in Firebase Console).
// Fill in with a test user that has permission under your Firestore rules.

export const DEV_AUTH_EMAIL = ""; // e.g., "dev@example.com"
export const DEV_AUTH_PASSWORD = ""; // e.g., "supersecret"
